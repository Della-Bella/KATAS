import { portfolioData } from './data.js'; 


// 1- Wait until the HTML document is fully loaded 
document.addEventListener('DOMContentLoaded', () => {
    console.log("Data imported:", portfolioData);

    3// Definir Array Data estrurura




//2- Create the const JavaScript :
    //menus with drop-downs-menu:
    const navItemsWithDropdown = document.querySelectorAll('.main-nav .has-dropdown');

    //sub categoris menus:
    const allCategoryTypes = document.querySelectorAll('.category-type'); // will get the all "querySelectorAll"/ array estrutura




});